# Deployed UI Audit

The `Deployed UI Audit` GitHub Actions workflow opens the public Vercel deployment in headless Chromium, captures key desktop and mobile states, and records console errors, runtime exceptions, failed requests, HTTP failures, canvas geometry, and resource timing information.

Target deployment:

- https://supermarket-restock-game.vercel.app
